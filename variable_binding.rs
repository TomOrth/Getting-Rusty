
fn main() {
    let x = 5; //variable declaration. type can be assumed
    let (a,b) = (2,3); //patterns allow for multi declaration
    let y: i32 = 5; //type annotations
    let mut c = 5; //mut makes a variable mutable. immutable by default
    c = 10;
}